# This project creates a library that contains all the game logic.
# It will then be liked to an executable that specifies the Renderer. 
# A TerminalRenderer and a GUIRendererSDL is planned for now.
